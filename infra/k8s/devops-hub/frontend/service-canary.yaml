apiVersion: v1
kind: Service
metadata:
  name: frontend-canary
  namespace: devops-hub
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
