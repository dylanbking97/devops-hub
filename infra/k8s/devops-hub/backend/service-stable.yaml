apiVersion: v1
kind: Service
metadata:
  name: backend-stable
  namespace: devops-hub
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    # Named "http" port required for ServiceMonitor port reference.
    - name: http
      port: 8000
      targetPort: 8000
